<view class="card profile-header-card">
  <view class="row">
    <view class="flex-column" style="flex: 1;">
      <view class="text-title">{{profile.merchantName || '商家'}}</view>
      <view class="badge-group" style="margin-top: 16rpx;">
        <view class="badge badge-info" wx:if="{{profile.merchantType}}">
          {{profile.merchantType}}
        </view>
        <view class="badge {{profile.status === 1 ? 'badge-success' : 'badge-danger'}}" wx:if="{{profile.status !== undefined}}">
          {{profile.status === 1 ? '已启用' : '已禁用'}}
        </view>
      </view>
    </view>
    <view class="logout-btn-wrapper">
      <view class="logout-btn" bindtap="onLogout">
        <text>退出</text>
      </view>
    </view>
  </view>
</view>

<view class="card info-card">
  <view class="text-subtitle" style="margin-bottom: 24rpx;">📋 账户信息</view>
  
  <view class="info-item">
    <text class="muted">商家ID</text>
    <text class="text-subtitle">{{profile.merchantId || '-'}}</text>
  </view>
  
  <view class="divider" wx:if="{{profile.resourceId}}"></view>
  
  <view class="info-item" wx:if="{{profile.resourceId}}">
    <text class="muted">资源ID</text>
    <text class="text-subtitle">{{profile.resourceId}}</text>
  </view>
  
  <view class="divider" wx:if="{{profile.resourceId && profile.createdAt}}"></view>
  
  <view class="info-item" wx:if="{{profile.createdAt}}">
    <text class="muted">创建时间</text>
    <text class="text-subtitle">{{profile.createdAt}}</text>
  </view>
  
  <view class="divider" wx:if="{{profile.createdAt && profile.resourceId}}"></view>
  
  <view wx:if="{{profile.resourceId}}" style="margin-top: 24rpx;">
    <button class="btn-primary" style="width: 100%;" bindtap="onEditResource">
      📝 编辑资源信息
    </button>
  </view>
</view>


