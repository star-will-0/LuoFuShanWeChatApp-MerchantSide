<view class="login-container">
  <view class="login-header">
    <view class="login-logo">🏪</view>
    <view class="text-title" style="margin-top: 24rpx;">商家端登录</view>
    <view class="muted" style="margin-top: 12rpx;">请使用管理端创建的账号登录</view>
  </view>

  <view class="card" style="margin-top: 60rpx;">
    <view class="input-group">
      <view class="text-label">账号</view>
      <input 
        placeholder="请输入商家账号" 
        value="{{username}}" 
        bindinput="onUsernameInput"
        maxlength="64"
      />
    </view>

    <view class="input-group" style="margin-top: 32rpx;">
      <view class="text-label">密码</view>
      <input 
        placeholder="请输入密码" 
        password 
        value="{{password}}" 
        bindinput="onPasswordInput"
        maxlength="128"
      />
    </view>

    <button 
      class="btn-primary" 
      style="margin-top: 48rpx; width: 100%;" 
      loading="{{loading}}" 
      disabled="{{loading}}" 
      bindtap="onLogin"
    >
      {{loading ? '登录中...' : '登录'}}
    </button>
  </view>

  <view class="login-tips">
    <view class="muted" style="text-align: center; font-size: 24rpx;">
      💡 提示：商家账号由管理端创建，如有问题请联系管理员
    </view>
  </view>
</view>


